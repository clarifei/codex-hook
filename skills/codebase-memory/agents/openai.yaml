interface:
  display_name: "Codebase Memory"
  short_description: "Use the codebase-memory MCP for repository context"
  default_prompt: "Use $codebase-memory to inspect this repository through the codebase-memory-mcp server before making changes."

dependencies:
  tools:
    - type: "mcp"
      value: "codebase-memory-mcp"
      description: "Repository code graph, semantic search, and impact analysis MCP server"

policy:
  allow_implicit_invocation: true
